// Route transitions are handled via CSS (@keyframes page-enter in styles.css).
// The .page class on every route component automatically gets the animation.
// This file is kept as a placeholder — no deprecated Angular Animation APIs used.
export {};
